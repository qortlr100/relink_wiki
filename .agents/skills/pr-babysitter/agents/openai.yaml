interface:
  display_name: "PR 자동 감시 및 병합"
  short_description: "외부 리뷰와 CI 확인 후 안전하게 자동 병합"
  default_prompt: "Use $pr-babysitter to monitor this draft PR and enable auto-merge only after external review and CI are clean."
